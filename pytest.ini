[pytest]
; restrict collection to tests/ so the legacy root-level test.py is not collected
testpaths = tests
filterwarnings =
    ignore::SyntaxWarning
    ignore::DeprecationWarning
    ignore::UserWarning
